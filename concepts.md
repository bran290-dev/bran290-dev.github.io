---
layout: default
title: 핵심 개념
nav_order: 1
permalink: /concepts/
---

# 핵심 개념

Transy에서 사용되는 주요 용어를 설명합니다.

---

## API 키 (API Key)

AI 번역 서비스를 사용하기 위한 인증 키입니다. Google AI Studio에서 무료로 발급받을 수 있으며, 앱 설정에서 등록합니다.

> [API 키 설정 가이드](getting-started/api-key-setup.md)

---

## 청크 (Chunk)

긴 텍스트를 AI에 보내기 위해 나누는 단위입니다. AI 모델에는 한 번에 처리할 수 있는 텍스트 길이 제한이 있어, 긴 회차는 여러 청크로 나누어 번역합니다. 번역 진행도에 "청크 3/7 (42%)"처럼 표시됩니다.

---

## 컨텍스트 (Context)

번역 품질을 높이기 위해 AI에게 제공하는 배경 정보입니다. 캐릭터 이름, 용어, 관계, 사건 등을 등록하면 AI가 이를 참고하여 더 정확하고 일관된 번역을 생성합니다.

> [컨텍스트 시스템 개요](context/overview.md)

---

## 컨텍스트 윈도우 (Context Window)

번역 시 AI에게 참고로 제공하는 이전 회차의 범위입니다. 예를 들어 "윈도우 크기: 3"이면 현재 회차 번역 시 이전 3개 회차의 요약이 함께 전달됩니다. 윈도우가 클수록 맥락 파악이 좋지만 API 비용이 증가합니다.

---

## API 설정 체인 / 페일오버 (Fallback Chain)

여러 API 설정을 순서대로 배치하여, 첫 번째 설정이 실패(할당량 초과, 오류 등)하면 자동으로 다음 설정으로 전환하는 기능입니다. 번역 중단 없이 안정적으로 서비스를 유지할 수 있습니다.

> [API 설정 체인 가이드](advanced/fallback-chain.md)

---

## 파싱 룰 (Parsing Rule)

웹소설 사이트에서 소설 정보와 본문을 자동으로 추출하기 위한 규칙입니다. 사이트마다 HTML 구조가 다르므로, 각 사이트에 맞는 파싱 룰이 필요합니다. AI가 자동으로 생성하거나 직접 만들 수 있습니다. 파일(EPUB/TXT)이나 텍스트 직접 입력 시에는 파싱 룰이 필요 없습니다.

> [파싱 룰 생성 가이드](advanced/parsing-rules.md)

---

## 프리패치 / 미리 받기 (Prefetch)

현재 읽고 있는 회차의 다음 회차들을 백그라운드에서 미리 다운로드하고 번역하는 기능입니다. 회차를 넘길 때 대기 시간 없이 바로 읽을 수 있습니다.

> [미리 받기 가이드](reading/prefetch.md)

---

## 전처리 (Preprocessing)

번역 전에 원문을 정리하는 과정입니다. 광고, 작가 노트, 반복되는 머리말/꼬리말 등 불필요한 텍스트를 자동으로 제거하여 번역 품질과 효율을 높입니다. 소설별 설정에서 필터 강도를 조절할 수 있습니다.

---

## 할당량 / 속도 제한 (Rate Limit)

API 제공사가 설정한 사용량 한도입니다. 무료 티어에서는 분당/일당 요청 횟수가 제한됩니다. 한도를 초과하면 일정 시간 후 자동으로 해제되며, API 설정 체인을 활용하면 다른 모델로 자동 전환할 수 있습니다.

---

## 안전 필터 (Safety Filter)

AI 모델이 폭력, 혐오 등 부적절한 콘텐츠를 감지하여 번역을 차단하는 기능입니다. API 제공사의 정책에 의해 작동하며, 앱에서 해제할 수 없습니다. 차단이 발생하면 자동 재시도 후 다른 모델로의 전환을 권장합니다.

---

## 토큰 (Token)

AI가 텍스트를 처리하는 최소 단위입니다. 한글 1글자는 약 2~3토큰, 영어 단어 하나는 약 1~2토큰에 해당합니다. API 비용은 토큰 수에 비례하므로, 토큰 수를 알면 대략적인 비용을 예측할 수 있습니다.

> [API 사용량 모니터링](advanced/api-usage.md)

---

## 회차 분할 (Chapter Split)

긴 회차를 AI가 처리하기 적절한 크기로 자동 분할하는 기능입니다. 분할된 회차는 "제2화-1", "제2화-2" 형태로 표시됩니다. AI 모델의 성능에 따라 권장 분할 크기가 다릅니다.

> [회차 분할 가이드](reading/chapter-split.md)

---

[← 목차로](index.md)
